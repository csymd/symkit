# Staff package

Common instructional-staff rules. Install locally with `instructor` or `ta`.
Do not commit to student-facing trees.

`evaluate-content` and `write-gherkin` live in `core/library/skills/` and
are assigned to instructor/TA (and materials, for Gherkin) in `catalog.yaml`.

| Contents | Path |
|:---------|:-----|
| Engagement norms | `.agents/rules/student-engagement.md` |
