# Staff package

Common instructional-staff rules and skills. Install locally with `instructor`
or `ta`. Do not commit to student-facing trees.

| Contents | Path |
|:---------|:-----|
| Engagement norms | `.agents/rules/student-engagement.md` |
| `evaluate-content` | `.agents/skills/evaluate-content/` |
| `write-gherkin` | `.agents/skills/write-gherkin/` |
