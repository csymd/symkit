# Teaching shared package

Student-safe materials defaults: `AGENTS.md`, course-materials rules, and
the `materials-author` agent. `release-materials` and `write-gherkin` are
assigned to the materials role in `catalog.yaml`.

Literacy handouts live in the **learner** package, not here.
