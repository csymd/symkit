# Experiment workspace

Scaffolded by [symkit](https://github.com/csymd/symkit) (`ai` harness).

| Path | Purpose |
|:-----|:--------|
| `config.yaml` | Models, phases, seeds |
| `scripts/` | Run and metric entrypoints |
| `experiments/` | Per-run outputs |
| `metrics/` | Aggregated tables |
