# Biosignal harness (later)

Not installable in v1. Reserved for agents and workspace conventions around
physiological / biomechanical signals and [SymWorx](https://github.com/symworx/symworx).

When this ships it should cover:

- Signal quality and provenance
- Calling shared kernels instead of reimplementing them
- Public-sample vs restricted device-data boundaries

Until then, use `research` or `ai` and keep domain rules in the target repo.
